dist
node_modules
coverage
*.d.ts
*.tsbuildinfo
package-lock.json

# Producer-authored PDPP source declarations, vendored byte-for-byte.
# The declaration digest is computed over these files' EXACT BYTES, so
# reformatting them silently invalidates every producer digest an import
# verifies against -- surfacing later as an unexplained digest_mismatch
# rather than as a diff anyone reads. See __fixtures__/PROVENANCE.md.
packages/core/src/pdpp/__fixtures__/*.source-declaration.json
