# Claude Fable — launch orchestrator handoff, September 14

Start release preparation now. Kahtaf owns canonical release decisions; you orchestrate delegates. Read the [runbook](/Users/kahtaf/Documents/workspace_vana/e2e-proof-2026-09-09/fleet-overnight/launch-handoff-260914/orchestrator-runbook.md) and [source inventory](/Users/kahtaf/Documents/workspace_vana/e2e-proof-2026-09-09/fleet-overnight/launch-handoff-260914/source-inventory.md). Do not repeat architecture review or ask all six old decisions.

**Precedence/state:** September 14 direction and the updated [production plan](/Users/kahtaf/Documents/workspace_vana/personal-server-ts/docs/260906-production-plan.md) supersede old “no flags,” “mainnet excluded,” and review gates. Memories are historical operations references. Review is complete; flag/restoration code and new live proof are not implemented. These documents are uncommitted: read their original absolute paths or carry copies; new worktrees will not contain them automatically.

**Target:** deploy mainnet with working Lite and TEE off; enable limited Moksha TEE. Separately prove **actual chain 1480 mainnet TEE** for selected wallets, then expand gradually. Retire the flag, remove Lite after consumers are ready, then clean obsolete branches/relay infrastructure. PS retains Lite; Unity removed consumer adapters, so hiding the enclave panel is insufficient.

**One flag:** recommend one Account-server policy keyed by chain/authenticated owner allowlist. Initially 1480 = legacy; 14800 = selected TEE. One Account redeploy toggles policy; no new platform or independent service booleans. Gateway enforces the same bounded-validity admission decision; UI receives effective eligibility.

**Off:** deny new TEE setup; preserve existing identities, recorded DCR/MCP routing and status/revoke controls. Unknown/prepared-only state is not enrolled or automatically legacy. Policy failure denies new setup; existing authorized routing continues. Never silently fall back after TEE failure. Explicit test-access disable/revoke is separate from rollout-off.

## Next actions

1. **Isolated branches/dependencies — 10–20 min.** Use the fresh inventory; recheck only changed refs, then create isolated branches and verify dependency setup. Branch from verified parent heads, not stale detached checkouts. Preserve dirty docs and PS #299/Unity #1043 removal work. Record unresolved canonical inputs while continuing local preparation.
2. **Policy and working off path — estimated 1–3 engineering days.** Delegate contract first, then Account/Gateway/Unity slices with focused failing tests before behavior changes. Cover off/on, selected/unselected, unknown/unavailable policy, wrong chain/network switch, client overrides, existing-owner off, recorded DCR/MCP continuity. Restore actual Lite collection/readiness/consent/MCP adapters; enforce TEE signing/admission boundaries. Use capable coding agents and independent high-reasoning review available in your harness; Fable coordinates.
3. **Release/off-on proof — 0.5–1 day after code.** Run gates/internal review; fix shared production/off/on/smoke blockers. Prepare SDK → Gateway migrations/deploy → PS → Account/Unity. Prove legacy default and limited Moksha TEE on exact candidates. Request targeted go only when canonical changes are concrete and reviewable.
4. **Actual-mainnet smoke separately — estimated 1–3 engineering days plus approved operations.** Implement chain 1480 support across fleet/Account hard 14800 guards, contracts, anchors, keys and endpoints. Obtain target/wallet/policy authorization. Prove selected cold/warm read/MCP, unselected/wrong-chain denial, off/on continuity and explicit post-disable denial. This must not delay mainnet-off deployment or Moksha launch.
5. **Expand/retire/clean — after cohort evidence.** Grow mainnet gradually; retire the flag, verify Web/mobile/Desktop/SDK readiness, remove Lite, then propose scoped cleanup. Defer quotas, renewal refactor, HA, hot directory, outbox and reconciler/trust redesign unless a concrete selected-flow failure requires them. Small scope/peer fixes may run in parallel without gating launch.

**Done:** candidate refs/PRs, green gates and exact-target off/on receipts; separate actual-mainnet smoke when approved. Distinguish prepared/deployed/proven. Keep existing Gateway-revocation/operator-KMS trust without stronger claims.

**Pause boundaries:** main/dev merges; canonical host/DB changes; shared Privy signer/any policy; verifier/peer policy; project creation; credentials/OTP; unapproved deletion. Prepare diffs and continue independent work. Disposable preview permission is not blanket shared deletion. Never autoreview egress or external messages; internal review allowed. PS: build/test/lint:eslint/format:check; SDK: test:coverage; Unity: actual scripts, no plain pnpm install. Fold approved children `--no-ff` only into parent feature PRs; canonical merges need targeted go.
