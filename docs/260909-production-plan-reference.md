# Production plan — enclave jobs and MCP fleet (updated 2026-09-09)

Runbook for the Moksha rollout, followed by a separately gated mainnet rollout. **The September 9 fleet/MCP additions in §11 supersede the earlier single-worker provisioning, environment, admission and rollback instructions where they differ.** This document is a plan and evidence checkpoint, not authorization to deploy or merge.

The original September 6 cross-surface audit covered personal-server-ts #245, vana-sdk #211, data-gateway #100, vana-storage #24, unity-surfaces #987 and lorebook #1. Its PR states, environment inventories, line references and unresolved findings below are historical and must be rechecked before release. The storage base correction remains: #24 was based on `dev`, not `main`.

**Latest evidence incorporated: 2026-09-09 18:52 UTC.** The Moksha pilot is active with one controller and two correctly named, healthy workers. Fresh Claude OAuth/native reads, same-owner SDK jobs with actual HTTP200 consumer acknowledgments, shared generation-10 placement, and MCP failover from a stopped W1 at generation11 to W2 at generation12 have passed. W1 restarted and was mutually re-admitted with a new incarnation. These are pilot results, not a completed production or mainnet rollout. The final evidence is in `/Users/kahtaf/Documents/workspace_vana/e2e-proof-2026-09-09/fleet-overnight/results.md`. Secret names/references only; never place secret values in this plan.

**Production readiness direction:** §12 supersedes the pilot's daily signed-config renewal requirement and the legacy single-worker migration/rollback acceptance gates for a net-new launch. Earlier sequences remain historical recovery references. The user requires a durable production setup without temporary renewal chores.

## 0. Blockers found while verifying (read first)

| #   | Finding                                                                                                                                                                                                                                                                                     | Evidence                                                                                                                                                                                                                   | Impact                                                                                                                                                                                                                                                  |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1  | **SDK enclave trust anchors are empty and production ignores the env override.** `ENCLAVE_TRUST_ANCHORS` is `emptyAnchor()` for both 1480 and 14800; `verifyEnclaveIdentityEvidence` throws `KMS root trust anchor is not provisioned`. Web/Account read the override only when env ≠ prod. | `vana-sdk packages/vana-sdk/src/protocol/identity.ts:185-192, 318-320`; `unity-surfaces apps/web/src/features/personal-server/enclave/enclave-client.ts:250-258`; `apps/account/src/lib/signing/enclave-delivery.ts:61-73` | **Two SDK releases required.** Release #1 unblocks the image build; the CVM must be provisioned to learn `app_id` + KMS root; release #2 bakes those in; then re-pin and redeploy Web/Account. Without it every production enclave verify fails closed. |
| B2  | vana-storage **production** `GATEWAY_URL_MOKSHA = https://dp-rpc-dev.vana.org`, but Web on moksha talks to `https://dp-rpc.moksha.vana.org`. Job-result PUT auth is verified per chain against that URL.                                                                                    | `vana-storage wrangler.toml [env.production].vars`; `src/chain.ts:61-62`; `unity-surfaces packages/app-runtime/src/personal-server/environments.json` (`dev.gatewayUrl`)                                                   | If the two hosts are not aliases of the Vercel project `dp-rpc-moksha`, chain-14800 result uploads 401 in production. Both hosts answer `{"status":"ok"}`; alias identity **[UNVERIFIED]**. Confirm, or change the committed prod var.                  |
| B3  | data-gateway migrations **0048–0051 "have not been applied to any environment yet"**, and the repo rule is "apply every earlier unapplied migration in numeric order". The enclave checklist only names 0053–0056.                                                                          | `data-gateway db/migrations/README.md`                                                                                                                                                                                     | Applying 0053+ on a DB missing 0048–0051 leaves the app reading columns/tables that do not exist. Inventory both Neon branches first.                                                                                                                   |
| B4  | `apps/account/migrations/MANIFEST.md` still says "**treat migrations as local-development scaffolding only**" pending three owner confirmations (canonical `008_*` filename, `_migrations` ledger mismatch, `010` ordering).                                                                | `unity-surfaces apps/account/migrations/MANIFEST.md`                                                                                                                                                                       | 026/027 are safe in isolation, but the runner walks the whole directory. Resolve or pre-seed the `_migrations` ledger before `--allow-remote`.                                                                                                          |
| B5  | `environments.json` `prod` and `dev` carry **identical contract addresses**; only `chainId`/URLs differ.                                                                                                                                                                                    | `packages/app-runtime/src/personal-server/environments.json`                                                                                                                                                               | Either the contracts really are address-identical across chains, or mainnet is misconfigured. **[UNVERIFIED]** — confirm before the mainnet leg.                                                                                                        |
| B6  | Agent still **builds from source at CVM boot** (`git fetch` → `npm ci` → `npm run build`) despite the digest-pinned `AGENT_IMAGE`.                                                                                                                                                          | `deploy/dstack/docker-compose.enclave.yml` agent `command`                                                                                                                                                                 | `GIT_REF` must be the merged `main` SHA and must be fetchable; boot time and npm-registry availability are in the critical path of every provision.                                                                                                     |
| B7  | `vana-web-prod` already has `NEXT_PUBLIC_PS_MAINNET_DISABLED` set (value not read).                                                                                                                                                                                                         | `vercel env ls --project vana-web-prod`                                                                                                                                                                                    | If it is `"true"`, `app.vana.org` already resolves mainnet → moksha, which is exactly what the moksha-first rollout wants. Verify the value before planning the mainnet leg.                                                                            |

## 1. Cross-surface order (moksha first; mainnet gated on it)

This is the original release dependency order. Integrate and release the reviewed fleet changes in personal-server-ts #273 and data-gateway #118 into their respective feature parents before the production leg; do not deploy the historical parents alone and expect fleet support. Use §11 for fleet provisioning, migration, admission, DNS and recovery. Recheck all release refs and SDK pins.

1. Merge **vana-sdk #211 → main**. `release.yml` runs `semantic-release` on push to main → publishes `@opendatalabs/vana-sdk` (version decided by conventional commits). Record it.
2. Re-pin that version in **personal-server-ts #245** (5 manifests + lockfile), **data-gateway #100** (devDep), **lorebook #1**. Re-run each CI.
3. Apply data-gateway migrations to the **moksha** Neon branch: the 0048–0051 backlog (B3) if unapplied, then `0053`, `0054`, `0055`, `0056` in order, out of band, statement-by-statement.
4. Add the 7 gateway env vars to Vercel project **dp-rpc-moksha** (production) with `PREWARM_ENABLED` **unset/false**.
5. Merge **data-gateway #100 → main**; confirm `dp-rpc.moksha.vana.org` deploy and that `/v1/cron/jobs-sweep` is registered.
6. Merge **vana-storage #24 → dev** (auto-deploys dev), settle B2, then **dev → main** (`deploy-prod.yml`, `wrangler deploy --env production`).
7. Add the **R2 lifecycle rule** on prefix `jobresults/` to bucket `vana-storage` (and `vana-storage-dev`). Manual; no wrangler support.
8. Merge **personal-server-ts #245 → main**. `docker.yml` on `main` pushes `vanaorg/personal-server:<sha>` and emits the `images.env` artifact. Download it.
9. Provision the moksha CVM(s) on Phala with `GIT_REF` = that main SHA, `PS_IMAGE`/`PS_IMAGE_REF` from `images.env`, digest-pinned `AGENT_IMAGE`/`DIND_IMAGE`. Then `replicate.sh` for each extra node (same `app_id`).
10. Capture `app_id` and the uncompressed KMS root (`node scripts/tee/kms-root.mjs`). Set `ENCLAVE_KMS_ROOT_PUBKEY` + `ENCLAVE_APP_ID_ALLOWLIST` on **dp-rpc-moksha**; redeploy.
11. Register each node (`POST /v1/tee-nodes`, operator bearer), wait for a heartbeat carrying the expected compose hash, then `POST /v1/tee-nodes/<id>/admit`.
12. **SDK release #2** filling `ENCLAVE_TRUST_ANCHORS[14800]` from step 10 (B1). Re-pin it everywhere from step 2.
13. Apply account migrations `026` then `027` to the production account DB; add the Web/Account env vars; merge **unity-surfaces #987 → dev**, then **dev → main** (`vana-web-prod`, `vana-account-prod`).
14. Update the **Privy policy** to the 5-rule shape (§6) before Mobile sign-in traffic; validate-only, manual in the dashboard.
15. Merge **lorebook #1 → main**, set its env, provision Redis. Then run the verification gate (§7) and only afterwards repeat 3–13 for **mainnet** (§8).

## 2. Per-surface counts

Historical single-worker estimates only. They exclude the controller, fleet Gateway settings, migrations 0057/0058, signed role configurations, DNS/TLS and protected-state recovery in §11; do not use these totals as the current fleet checklist.

| Surface                            | Env vars to add/change                                        | Migrations                                            | One-time manual steps                                    |
| ---------------------------------- | ------------------------------------------------------------- | ----------------------------------------------------- | -------------------------------------------------------- |
| vana-sdk (npm)                     | 0                                                             | 0                                                     | 3 (merge-release, record version, second anchor release) |
| personal-server-ts (image + CVMs)  | 2 GitHub repo (verify) + 9 required Phala + 14 optional Phala | 0                                                     | 6                                                        |
| data-gateway (Vercel ×2 + Neon ×2) | 7 add × 2 projects = 14; 1 possible change                    | 4 files × 2 Neon branches = 8 (+ up to 4 backlog × 2) | 0                                                        |
| vana-storage (Worker + R2)         | 1 change, committed in `wrangler.toml`                        | 0                                                     | 1 (R2 lifecycle rule)                                    |
| unity-surfaces (web/account)       | 2 add (web) + 1 optional (account); 1 to verify               | 2 files, 1 database                                   | 2 (Privy policy, Hydra redirect check)                   |
| lorebook (Vercel + Redis)          | 5 add (2 optional)                                            | 0                                                     | 1 (Redis provisioning)                                   |
| **Total (moksha leg)**             | **~33**                                                       | **10**                                                | **13**                                                   |

## 3. Env vars — names, location, verification

**data-gateway** — Vercel project env, `production` scope, on **both** `dp-rpc-moksha` and `dp-rpc`. All 7 were reported absent in the September 6 audit; recheck before deployment (`vercel env ls production --scope opendatalabs --project …`):

`OPERATOR_SECRET`, `STORAGE_API_URL`, `ENCLAVE_AGENT_SECRET`, `ENCLAVE_KMS_ROOT_PUBKEY`, `ENCLAVE_APP_ID_ALLOWLIST`, `PREWARM_ENABLED`, `ENCLAVE_AGENT_URL` (dev-only convenience — `lib/tee/agent-selection.ts:22` says production talks only to admitted nodes, so **leave it unset in production**).

Read sites: `lib/operator-auth.ts:47` (falls back to `CRON_SECRET`), `lib/jobs/config.ts:5` (https, no trailing slash, throws if unset), `lib/enclave-agent.ts:72`, `lib/tee/anchors.ts:15-30`, `lib/prewarm.ts:19` (`=== "true"`). `.env.example` also re-comments `DATA_REGISTRY_CONTRACT` as "DataRegistryV2 on Moksha" — treat as a **change** to verify against the deployed value. `DOPPLER_PROJECT`/`DOPPLER_ENVIRONMENT`/`DOPPLER_CONFIG` already exist on both projects; confirm whether Doppler is the source of truth before editing in the Vercel UI.

**personal-server-ts — legacy deployment only.** For the fleet, use the measured compose and signed role configuration in §11; the following raw encrypted-env inventory is not a fleet boot contract. GitHub repo settings: `vars.DOCKERHUB_USERNAME`, `secrets.DOCKERHUB_SERVICE_ACCOUNT_PAT` (`.github/workflows/docker.yml`). Phala encrypted env, per CVM, required: `ENCLAVE_AGENT_SECRET`, `GIT_REF`, `GATEWAY_URL`, `NODE_ID`, `NODE_SECRET`, `PS_IMAGE`, `PS_IMAGE_REF`, `AGENT_IMAGE`, `DIND_IMAGE`. Optional (defaults in `scripts/tee/README.md`): `CHAIN_ID` (14800), `STORAGE_API_URL` (follows chain), `SANDBOX_MAX` (20), `SANDBOX_IDLE_TTL_SECONDS` (600), `LEASE_SECONDS` (30), `SANDBOX_MEMORY` (512m), `SANDBOX_CPUS` (2), `SANDBOX_PIDS_LIMIT` (256), `SANDBOX_SYNC`, `JOB_RESULT_MAX_BYTES`, `VERCEL_PROTECTION_BYPASS`, and the four `DATA_*_CONTRACT` overrides. Set `SANDBOX_DEBUG=0`. **Leave `WORK_DELAY_MS` unset** — it is the lease-recovery test lever only.

**vana-storage** — no new code-required vars. `preview_urls=true` is now on the default env with an explicit `preview_urls=false` override on production; the spike's `--var STORAGE_ORIGIN/LEGACY_STORAGE_ORIGINS/GATEWAY_URL_MOKSHA` were preview-only. Only B2 needs a decision. Secret: `CF_API_TOKEN` in GitHub (already used by `deploy-dev.yml`).

**unity-surfaces** — `vana-web-prod`: add `NEXT_PUBLIC_PS_ENCLAVE_ENABLED`, `NEXT_PUBLIC_ENCLAVE_PREWARM`; verify `NEXT_PUBLIC_PS_MAINNET_DISABLED` (B7). `vana-account-prod`: `ACCOUNT_DATA_PORTABILITY_PERMISSIONS_CONTRACT` is **optional** — `constrained-silent-signing.ts:470-473` falls back to the SDK address. `NEXT_PUBLIC_PS_ENCLAVE_ANCHOR_OVERRIDE` / `ACCOUNT_PS_ENCLAVE_ANCHOR_OVERRIDE` are **ignored in production** — do not set them (B1). `ACCOUNT_INTENT_SIGNING_ORIGINS`, `ACCOUNT_MOBILE_PRIVY_*`, `ACCOUNT_VANA_CHAIN_ID`, `ACCOUNT_DATA_PORTABILITY_SERVERS_CONTRACT`, `WEB_OAUTH_REDIRECT_URI` all already exist on the prod projects. **`environments.json` is untouched by #987** and already carries the real production values — no committed patch is needed (the brief's uncommitted patch was preview-only; the fold worktree is clean).

**lorebook** — `VANA_READ_MODE=enclave`, `VANA_GATEWAY_URL` (bare origin, no path/query), `APP_URL`; optional `VANA_ACCESS_REQUEST_BASE_URL`, `VANA_APPROVAL_APP_BASE_URL`, plus `VANA_DEFAULT_ENV` / `VANA_DEFAULT_NETWORK` server-side fallbacks. Redis vars pre-exist: `LOREBOOK_REDIS_REST_URL`/`_TOKEN` or `UPSTASH_REDIS_REST_URL`/`_TOKEN` (`src/lib/vana/delivery-store.ts:251-259`). No `lorebook` project under the `opendatalabs` or `data-dex` Vercel scopes **[UNVERIFIED]** — locate it before the env pass.

## 4. Migrations

Fleet additions: `0057_fleet.sql` then `0058_fleet_recovery.sql`, after the applicable earlier migrations. Both were applied atomically to the approved **pilot** database on September 9; this says nothing about production or mainnet migration state. Inventory each target independently, verify exact SQL and definitions, and retain the database fences on rollback. See §11.

**data-gateway** — hand-written SQL, applied **out of band**; `npm run db:migrate` (drizzle-kit) is explicitly _not_ the mechanism and the journal is stale. Credential: `NEON_URL` for the target Neon branch. Order: backlog (B3) → `0053_identity.sql` → `0054_jobs.sql` → `0055_grants_expires_at_timestamptz.sql` → `0056_identity_last_prewarm_at.sql`. All four are idempotent (verified: 4/4, 12/12, 1/1, 1/1 guarded statements). `0055` briefly takes an `ACCESS EXCLUSIVE` lock — quiet window, no long transactions. Apply **before** deploying, and `0056` before setting `PREWARM_ENABLED`.

**unity-surfaces (account DB)** — `ACCOUNT_DB_MIGRATE_ALLOW_REMOTE=true DATABASE_URL=… node apps/account/scripts/migrate-account-db.mjs --allow-remote`. Both flags are mandatory for a non-localhost host; the runner tracks applied filenames in `_migrations` and wraps each file in a transaction. `026` and `027` both `DROP CONSTRAINT IF EXISTS` then re-add `mobile_sessions_scope_check`; `027` supersedes `026`'s value, so both are re-runnable and order-tolerant. See B4 before running.

## 5. Manual steps

- **R2 lifecycle rule**, prefix `jobresults/`, bucket `vana-storage` (dashboard; the handoff explicitly left it to a human). Retention period is not specified anywhere in the repo — it is an open decision.
- **Node registration + admit**: `POST /v1/tee-nodes` with `Authorization: Bearer $OPERATOR_SECRET` and the payload printed by `provision.sh`/`replicate.sh` (paste the raw `NODE_SECRET` into the placeholder once), then `POST /v1/tee-nodes/<id>/admit` after a fresh heartbeat shows the expected compose hash.
- **Docker Hub credentials** in GitHub repo settings (see §3).
- **Hydra redirect URI**: the preview run added one by hand. `WEB_OAUTH_REDIRECT_URI` and `ACCOUNT_HYDRA_REDIRECT_ALLOWLIST` already exist on the prod projects; #987 adds no new redirect target I could find, so this should be a **verify**, not an add. **[UNVERIFIED]**
- **Privy policy** (§6).

## 6. Privy policy — exactly what the code validates

`apps/account/src/lib/mobile-attestation/mobile-signer-bootstrap.ts` is validate-only and fails Mobile sign-in closed with `mobile_signer_bootstrap_unavailable` on any mismatch. Policy object: `chain_type = "ethereum"`, `version = "1.0"`, `id = ACCOUNT_MOBILE_PRIVY_POLICY_IDS` (exactly one id), `owner_id = ACCOUNT_MOBILE_PRIVY_POLICY_OWNER_ID`, and **exactly 5 rules** (`SUPPORTED_VANA_CHAIN_IDS × 2 + 1`) — an extra rule fails validation.

1. One `personal_sign` rule, `action: ALLOW`, exactly one condition: `{field: "content", field_source: "message", operator: "eq", value: "vana-master-key-v1"}`.
   2–5. Four `eth_signTypedData_v4` rules, `action: ALLOW`, **three conditions each** — one `ServerRegistration` and one `PrewarmRequest` rule for each of `"1480"` and `"14800"` (chain ids are **strings**).

- `ServerRegistration` rule: `{chainId, ethereum_typed_data_domain, eq, "<chainId>"}`, `{verifyingContract, ethereum_typed_data_domain, eq, <ACCOUNT_DATA_PORTABILITY_SERVERS_CONTRACT>}`, `{serverUrl, ethereum_typed_data_message, starts_with, "https://", typed_data: {primary_type: "ServerRegistration", types: {ServerRegistration: [ownerAddress address, serverAddress address, publicKey string, serverUrl string]}}}`.
- `PrewarmRequest` rule: `{chainId, ethereum_typed_data_domain, eq, "<chainId>"}`, `{verifyingContract, ethereum_typed_data_domain, eq, <same contract>}`, `{chainId, ethereum_typed_data_message, eq, "<chainId>", typed_data: {primary_type: "PrewarmRequest", types: {PrewarmRequest: [ownerAddress address, chainId uint256, scope string, expiresAt uint256]}}}`.

The signer is also validated: `authorization_threshold = 1`, exactly one authorization key with a non-empty `public_key`, and `user_ids` absent or empty.

## 7. Verification gate

The checks below remain useful cross-surface checks. They are insufficient for fleet release: also complete every live gate in §11, including same-connection MCP continuity, co-placement, failover and protected rollback. Repository E2E tests are not live multi-TEE evidence.

| Check          | Command / surface                                                                                                                                                                                 |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SDK published  | `npm view @opendatalabs/vana-sdk version` matches the re-pinned value in all 8 manifests                                                                                                          |
| Gateway health | `GET https://dp-rpc.moksha.vana.org/` → `{"status":"ok",…}`; `/v1/tee-nodes` with the operator bearer lists the admitted node                                                                     |
| Agent health   | `GET $AGENT_URL/agent/v1/health` with `ENCLAVE_AGENT_SECRET` → baked `nodeId` matches the registered id, `draining: false`                                                                        |
| Storage        | PUT/GET a chain-14800 job result at `jobresults/14800/<uuid>` (closes B2)                                                                                                                         |
| Remote e2e     | `E2E_REMOTE=1 npm run e2e:job` from `personal-server-ts`, then the builder-only variant (`E2E_BUILDER_ONLY=1 E2E_SKIP_BUILDER_REGISTRATION=1` + `OWNER_ADDRESS`/`GRANT_ID`/`BUILDER_PRIVATE_KEY`) |
| Lease recovery | `E2E_REMOTE=1 E2E_RECOVERY=1 E2E_NODE_IDS=<slow>,<fast> npm run e2e:job` — **do not run on production nodes** (needs `WORK_DELAY_MS`)                                                             |
| Chrome gate    | Lorebook → DCR approval on `app.vana.org` → enclave delivery → portrait renders; mirror the shots in `e2e-proof-2026-09-04/hosted-e2e.md`                                                         |

## 8. What differs for mainnet

For the fleet, provision a separate mainnet controller app and worker app family; replicas share their own worker app identity, not the Moksha worker app or the controller app. Choose and review the mainnet MCP hostname explicitly; `mcp-dev.vana.org` is the Moksha pilot origin. Establish fresh measured policies, DNS/TXT/CAA, in-TEE ACME issuance and SDK anchors for that environment. The pilot previews, credentials, database and attestation identities must not be copied into mainnet configuration. Central HA, automated scaling and certificate failover remain separate work.

`CHAIN_ID=1480`; the mainnet contract set (B5); a **separate** `app_id` and KMS root from an independently provisioned CVM fleet — a fresh `provision.sh` deliberately produces a new `app_id`, so mainnet and moksha nodes cannot decrypt each other's jobs; `STORAGE_API_URL=https://storage.vana.org` and the `vana-storage` prod bucket; gateway project `dp-rpc` (`dp-rpc.vana.org`); `ENCLAVE_TRUST_ANCHORS[1480]` filled by a third SDK release (or the same one as step 12 if both fleets exist first); `NEXT_PUBLIC_PS_MAINNET_DISABLED` flipped off on `vana-web-prod`.

## 9. Rollback

**The Gateway and Personal Server rows below describe the pre-fleet rollout only. After a protected export has been attempted, use §11.6 instead.** Restoring an old Gateway deployment, booting a fence-unaware runtime, or destroying a state-bearing CVM is not a safe fleet rollback. Preserve current state and the database generation fences.

| Surface            | Rollback                                                                                                                                                                                                                                                                                        |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| vana-sdk           | Cannot unpublish; `npm deprecate` and re-pin consumers to `3.23.0-pr.211.fa01520`                                                                                                                                                                                                               |
| data-gateway       | Vercel instant rollback to the previous production deployment. Migrations are additive and stay; `0055`'s timestamptz conversion is **not** auto-reversible                                                                                                                                     |
| vana-storage       | `wrangler rollback` / redeploy the previous `main` commit; the lifecycle rule can be deleted independently                                                                                                                                                                                      |
| personal-server-ts | `POST /agent/v1/drain` on the new node, `POST /v1/tee-nodes/<id>/remove`, keep the previous node admitted; `destroy.sh` the new CVM                                                                                                                                                             |
| unity-surfaces     | Vercel rollback on `vana-web-prod`/`vana-account-prod`; or set `NEXT_PUBLIC_PS_ENCLAVE_ENABLED` to anything but `"true"` (flag-off is byte-identical to pre-enclave). Account migrations are constraint-widening; reverting the app is safe, reverting the constraint invalidates live sessions |
| lorebook           | Unset `VANA_READ_MODE` → direct Personal Server reads + escrow                                                                                                                                                                                                                                  |
| Kill switch        | `PREWARM_ENABLED` unset, `NEXT_PUBLIC_ENCLAVE_PREWARM` off, drain all nodes                                                                                                                                                                                                                     |

## 10. Open decisions

| #   | Question                                                             | Recommendation                                                                                                                                                                                                                 |
| --- | -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Ship with `PREWARM_ENABLED=true`?                                    | **No.** Launch off. It adds a `waitUntil` side effect on every grant approval and 0056 is only just applied. Flip on after a week of clean job telemetry.                                                                      |
| 2   | Ship #987 before the Privy policy is updated?                        | **No.** `mobile-signer-bootstrap` sits on the unconditional Mobile sign-in path and fails closed — shipping first breaks all Mobile sign-in. Update the policy, verify, then merge.                                            |
| 3   | `SANDBOX_MEMORY` 512m vs tmpfs 256m vs `JOB_RESULT_MAX_BYTES` 64 MiB | Keep 512m. But 256m tmpfs cannot hold a 64 MiB result plus working set comfortably — either lower `JOB_RESULT_MAX_BYTES` to ~32 MiB or raise tmpfs. Decide explicitly.                                                         |
| 4   | `SANDBOX_IDLE_TTL_SECONDS`                                           | Keep the 600 default for launch; it is the warm-path lever, revisit with real traffic.                                                                                                                                         |
| 5   | Node capacity / instance type                                        | Fleet pilot: one separate controller plus two workers sharing their worker `app_id`. All three currently use `tdx.small` (1 CPU, 2 GiB, 20 GiB); production sizing requires the live cold/warm and concurrency results in §11. |
| 6   | `--inline` or digest compose for production?                         | **Digest.** Architecture decision 23; `--inline` is the registry-free level-B variant and produces no reproducible compose hash.                                                                                               |
| 7   | Prebuilt agent image (it still builds at boot, B6)                   | Ship as-is for launch, file the follow-up. Boot-time `npm ci` puts the npm registry in the provisioning critical path.                                                                                                         |
| 8   | Attestation follow-up (literal digests in YAML)                      | Required for the fleet: literal source/image/operator-public-key pins in measured compose, signed configuration and strict fresh peer verification. Do not defer these gates.                                                  |
| 9   | Decision #8 — unauthenticated identity-secret POST to re-affirm      | Confirm the re-affirm path is idempotent and rate-limited before launch, or gate it behind the operator bearer.                                                                                                                |
| 10  | R2 `jobresults/` retention period                                    | Not specified anywhere in code. Pick one (suggest 7 days) — the rule cannot be created without it.                                                                                                                             |
| 11  | B2: is `dp-rpc-dev.vana.org` an alias of `dp-rpc-moksha`?            | If yes, no change. If no, change the committed prod `GATEWAY_URL_MOKSHA` to `https://dp-rpc.moksha.vana.org`.                                                                                                                  |
| 12  | B5: are mainnet contracts really the same addresses as moksha?       | Confirm from a deployment record before the mainnet leg.                                                                                                                                                                       |

## 11. MCP fleet infrastructure and operational plan (September 9)

### 11.1 Scope and evidence checkpoint

The architecture is one central TEE for public MCP ingress, encrypted OAuth/connection state and placement, plus two worker TEEs for owner sandboxes. MCP calls, SDK jobs and prewarm must use the same controller assignment. Worker replicas preserve the existing worker application identity so they can derive the same owner keys; the controller uses a separate application identity. The controller is currently a single availability dependency, not an HA deployment or an autoscaler.

Design and contracts: [controller design](260908-tee-fleet-controller-design.md), [implementation handoff](260908-tee-fleet-implementation-handoff.md). Timestamped pilot evidence lives outside source commits in [the run report](../../e2e-proof-2026-09-09/fleet-overnight/report.md) and [DNS/TLS gate](../../e2e-proof-2026-09-09/fleet-overnight/dns-tls-controller-gate-20260909T1558Z.json). Re-read current deployment metadata before an operation; the following is a checkpoint, not a claim that later stages have completed.

| Item                      | Recorded state at 15:58 UTC                                                                                                                       |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| Personal Server candidate | [#273](https://github.com/vana-com/personal-server-ts/pull/273), `79ca1fc5e29f8b7fed03e0a9ade8bf56a5b03ee7`                                       |
| Owner sandbox image       | `vanaorg/personal-server@sha256:0a6a46618f31dba6f4ffca87c7d33fd80fd516c61be59457872686f2708875c7`                                                 |
| Gateway candidate         | [#118](https://github.com/vana-com/data-gateway/pull/118), `1fb41854870208c246e824c0b9098105e81bde27`                                             |
| Source validation         | Node 20: 2,023 tests, 26 repository E2E and 15 checkout regressions; Node 22: 2,022 passed + one existing skip, 26 E2E; reviews clear             |
| Protected state           | One connection migrated directly between TEEs at 15:42:46–15:42:49 UTC; source durably fenced; import reports no restart required                 |
| Worker authority          | Both workers mutually authenticated to the controller and separately Gateway-admitted; historical `spike-mcp-demo-a1` is draining                 |
| Public ingress            | DNS points to the controller; verified HTTPS and unchanged OAuth issuer; unauthenticated MCP returns 401                                          |
| Remaining activation gate | Controller paused with no placements; activation, post-cutover application continuity and the full live fleet acceptance matrix are still pending |

The pilot source includes two observed restart fixes: scoped removal of stale Docker/containerd PID files, and guarded reconstruction of the disposable `/app` checkout after interrupted Git operations. Protected state volumes remain intact. Production still builds controller/agent code at boot, so Git/npm availability and boot latency remain operational dependencies.

### 11.2 Resource and endpoint inventory

All three pilot CVMs use production dstack 0.5.9, `tdx.small`, 1 vCPU, 2 GiB RAM and 20 GiB disk. This is the current test allocation, not a proven production capacity target. Each worker advertises capacity 2; the tested configuration uses 512 MiB per owner sandbox and a 30-second idle TTL.

| Role / logical node                 | CVM UUID                               | dstack instance                            |
| ----------------------------------- | -------------------------------------- | ------------------------------------------ |
| `moksha-personal-server-controller` | `ae2a9c3d-6163-4d94-a3e2-34c403eda4f7` | `6223bcb478a5f5b7d11dae5713d9ff58501c090a` |
| `moksha-personal-server-worker-1`   | `87c32ca4-49c9-4a83-b0af-ef5465cbbe18` | `1ac22335a2da1ca6a024396c4a557907279403ab` |
| `moksha-personal-server-worker-2`   | `1a5acf08-41b4-4524-9e0d-d8b520b5b9ae` | `eeea1588a1f55b129a2b14b24a1002719976f6a0` |

Controller app: `f8f00a86dbae7f4622e23d512d3929792c6fc9f4`. Both workers share app `ec9a39de98c760e1ded9f1e97016dc5f0e357cf2`. Worker-1 retained the original physical CVM/app/instance while moving to its new logical node ID and dedicated node credential. A display name is not an authenticated identity, and controller admission is separate from Gateway node admission.

Internal/operator endpoint pattern is `https://<instance>-<port>.dstack-pha-prod5.phala.network`: controller Gateway interface 8790, admin 8791, peer 8792; workers agent 8787 and peer 8789. Public MCP is only `https://mcp-dev.vana.org/mcp`; TLS terminates inside the controller CVM and forwards to private ingress port 8788. Ports and instance endpoints do not remove the need for service authentication and fresh peer attestation.

Preserve `mcp-state`, controller `fleet-state`, `mcp-certificates` and `mcp-evidence` volumes as applicable. Do not recreate the worker app, export owner keys or copy decrypted OAuth state onto the operator host to perform migration or recovery.

### 11.3 DNS and in-TEE TLS for `mcp-dev.vana.org`

Cloudflare is **DNS-only** (`proxied: false`). The dstack transport carries TLS to the selected instance; Cloudflare does not terminate this MCP TLS connection. The hostname, OAuth issuer and `/mcp` URL stay stable across the move from worker-1 to controller.

| Type / name                                | Controller value, verified September 9                                                                                         |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------ |
| CNAME `mcp-dev.vana.org`                   | `_.dstack-pha-prod5.phala.network` — unchanged                                                                                 |
| TXT `_dstack-app-address.mcp-dev.vana.org` | `6223bcb478a5f5b7d11dae5713d9ff58501c090a:443`                                                                                 |
| CAA `mcp-dev.vana.org`                     | `0 issue "letsencrypt.org;validationmethods=tls-alpn-01;accounturi=https://acme-v02.api.letsencrypt.org/acme/acct/3726249856"` |

All three records use provider TTL `Auto`. TXT selects an **instance**, not the worker app or a generic load-balanced endpoint. The CAA account URI belongs to the controller's in-TEE ACME account. Do not reuse the old account restriction for a new controller or export ACME private keys to make issuance work. The TLS container needs no DNS API credential.

The TXT/CAA changes were verified at the provider and authoritative nameserver by 15:56:43 UTC. Mixed cached TXT answers temporarily delayed TLS; native DNS verification completed at 15:57:47, then public resolvers and verified HTTPS passed at 15:58:11. DNS-save success alone is not the TLS gate: check authoritative and public answers, SNI/hostname verification, the served certificate and its TEE evidence, and the unchanged OAuth issuer. Do not bypass certificate verification to declare service restored.

Retain these **previous worker-1 recovery values**:

- TXT: `1ac22335a2da1ca6a024396c4a557907279403ab:443`.
- CAA: `0 issue "letsencrypt.org;validationmethods=tls-alpn-01;accounturi=https://acme-v02.api.letsencrypt.org/acme/acct/3725271346"`.
- CNAME and DNS-only mode stay unchanged.

Use those recovery values only after the state/authority recovery in §11.6. DNS failback alone cannot restore a fenced writer. Verify the actual retained source ACME account/certificate again before failback. DNS/WebPKI and the explicitly approved KMS CA remain trust assumptions; this does not establish wildcard-certificate exclusion, controller HA or automatic certificate failover.

### 11.4 Configuration, Gateway and attestation gates

The measured compose pins the exact source SHA, Node/runtime image digests, sandbox image and operator Ed25519 public key. The only deployment environment passthrough is `FLEET_SIGNED_CONFIG`, a canonical signed bundle transported as `base64:`. It binds purpose/version, role, app, instance, logical node and validity times, plus the complete approved role configuration. Operator drafts use credential references that are resolved privately when signing. Verify it before listeners or protected state are opened; no unsigned policy override. The current pilot bundles have a 23-hour validity window. Record each bundle's actual expiry and the re-sign/redeploy procedure: expiry is a future **startup** risk, not evidence that an already running process is automatically stopped.

Controller settings include `FLEET_WORKERS_JSON`, `CONTROLLER_TERM`, `FLEET_STATE_PATH`, `MCP_MIGRATION_REQUIRED`, stable MCP origin/approval URL/redirect URIs, Gateway URL and distinct admin/Gateway credentials. Worker settings include its dedicated `NODE_SECRET`, agent secret, `FLEET_ENABLED`, reciprocal `FLEET_PEER_POLICIES`, image and sandbox limits. The operator recovery credential stays off the CVMs. Keep credential values out of logs and deployment evidence.

Gateway fleet configuration adds `FLEET_ENABLED`, `FLEET_CONTROLLER_URL`, `FLEET_CONTROLLER_ADMIN_URL`, `FLEET_GATEWAY_TOKEN`, `FLEET_CONTROLLER_GATEWAY_TOKEN`, `FLEET_CONTROLLER_ADMIN_TOKEN` and a distinct `FLEET_RECOVERY_TOKEN`. Preserve the configured public request audience: pilot signed `GATEWAY_URL` / `GATEWAY_PUBLIC_ORIGIN` is `https://dp-rpc-moksha-spike-b3.vercel.app`, even when an immutable preview is used for operator readback. Audit both runtime and build environment, exact source/upload contents and backing database before alias changes.

| Pilot routing artifact                                                                                               | Purpose                                                                                   |
| -------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| [Fleet-enabled preview](https://dp-rpc-moksha-85qqalm4a-opendatalabs.vercel.app), `dpl_KueZgMtG96BSfDTufYwyBkUFGJEP` | Stable pilot alias switched here at 15:47:29 UTC                                          |
| [Recovery preview](https://dp-rpc-moksha-pv7ikcl1c-opendatalabs.vercel.app), `dpl_CTehYesvnAqEEnm4Cf5iEmo5Qtrx`      | Same reviewed source with only runtime/build `FLEET_ENABLED=false`; retained for rollback |

Migrations `0057_fleet.sql` and `0058_fleet_recovery.sql` were committed atomically on the approved pilot database at 05:34:06 UTC. Their database enforcement prevents older immutable Gateway deployments from bypassing fleet owner/generation fences. Recovery-envelope retrieval is restricted to the admitted designated rollback node, current sealed identity and current recovery authority; it is not a general envelope endpoint.

For every measured deployment change, verify actual provider manifest/allowed environment, exact app/instance/compose and native boot, then Intel DCAP, non-debug state, approved base/OS/CA measurements and complete event replay. Provider app-scoped APIs have returned another worker's genuine quote: reject the identity mismatch and obtain the exact-instance evidence. Certificate evidence does not prove the fresh peer protocol. Exercise the production nonce/key-bound encrypted exchange and negative binding/replay checks as separate gates. The verifier must support dstack's documented omitted runtime digests in raw `GetQuote` while preserving replay and strict policy checks.

### 11.5 Serialized activation and release sequence

1. Freeze reviewed source, image, manifests, reciprocal policies, signed bundles, deployment IDs, DNS before/after values and recovery references. Reconfirm the target database, worker metadata, identity epochs and jobs/leases. Stage only the intended resources; prove invalid configuration fails closed, then signed boot and actual mutual peer admission while paused.
2. Prepare worker-1 on its preserved app/instance with a new logical node; drain the historical node. Existing-node idempotent registration does not repin a changed compose. Register new Gateway nodes **pending** while the old direct selector is still public, so it cannot accidentally route work to them.
3. Fence the source writer durably and transfer protected state directly through the attested encrypted channel. Verify the import receipt and honor `restartRequired`. After any export attempt, assume protected-state recovery is required until exact state is established; do not boot an old fence-unaware build.
4. While the controller remains paused, switch the stable Gateway alias to the audited fleet-enabled candidate and verify the deployment, request audience, identity and private directory. Separately admit exact current worker nodes in the Gateway with fresh compose and incarnation checks.
5. Apply the reviewed TXT/CAA switch, keep DNS-only CNAME unchanged, and verify propagation, in-TEE certificate issuance and the unchanged OAuth issuer. A transient 401 on unauthenticated MCP is useful transport/auth evidence; it is not a successful user read.
6. Activate the controller only after all preceding gates pass. Activation reconciles latest sealed membership for imported approved owners; partial failure leaves it paused with no new placement. New fleet approvals also enroll membership before approval, without starting an owner sandbox.
7. Prove the original Claude connection and ordinary SDK consumer path through the new ingress. Correlate owner/epoch/node/incarnation/generation/controller term, actual readiness version and timings across controller, Gateway and worker. Finish the acceptance matrix below before declaring the rollout complete or proceeding to production/mainnet.

At the recorded checkpoint steps 1–5 have passed for this pilot; steps 6–7 remain pending. Recheck the run record for later outcomes rather than inferring activation from DNS or node admission.

### 11.6 Fleet rollback and forward recovery

Rollback restores legacy service on the original physical worker with the **current reviewed build**, not historical binaries or a stale state snapshot. The new logical worker-1 node is the designated `rollbackNodeId`.

1. Persist controller pause, reconcile current sealed epochs for approved owners and drain. Wait for positive release or all conservative placement/job deadlines; a nominal 30-second lease is not a fixed sleep instruction. Preserve actual enrolled-owner rows and generations, including partially acknowledged enrollment.
2. Fence the controller writer and reverse-transfer the current protected state into the attested source worker. Honor the import receipt's restart requirement. No connection private keys or owner signatures leave the TEEs.
3. Apply recovery compare-and-swap for each actual enrolled owner using its current epoch, retained generation and exact rollback node. Verify `rollback_legacy`, no live assignment/lease, and no competing job authority. Do not delete enrollment, reset generations or remove database fences.
4. Call private `prepare-rollback` while the source still runs its fleet configuration. It resolves every imported approved owner's current identity and authorized recovery envelope, verifies derivation/unseal in the TEE, and commits the complete cache and migration-bound receipt atomically. Missing or stale material fails closed; no SDK warmup workaround is needed.
5. Make every other node ineligible for direct legacy selection, including worker-2 and the historical a1 row. Switch the Gateway alias to the reviewed **fleet-disabled recovery preview**, preserving recovery endpoints/credentials. A fleet-enabled Gateway would route new-owner setup/sealing/prewarm to the paused controller; an old Gateway would lack the first-boot recovery endpoint.
6. Start source worker-1 with the reviewed signed `FLEET_ENABLED=false` configuration. Its first recovered boot verifies the receipt and current recovery authority before opening service, then durably marks that migration activated. Later legitimate legacy approvals, revocations and epoch changes must survive restart; a subsequent export still fences the source and a new import resets activation.
7. Restore the previous source TXT/CAA only after state and authority are ready. Verify TLS, original Claude authorization, MCP-only wake, SDK result/ACK, fresh-owner setup and a later source restart. Retain evidence and resources needed for recovery until these pass.

Forward recovery repeats protected source fencing/import, uses explicit Gateway resume against retained generations, restores the fleet-enabled alias and controller DNS, re-admits intentionally drained workers as required, then activates. The next assignment generation must increase. DNS or alias restoration never substitutes for state transfer or writer fencing.

### 11.7 Outstanding live acceptance and production decisions

Already established: baseline owner ingestion/version update/signature/readback, existing Claude authorization and SDK result/ACK; real mutual peer authentication and negative session-binding checks; ownerless signed worker restart with changed incarnation and stale-incarnation rejection; protected forward migration; Gateway admissions; controller DNS/TLS. Local tests additionally cover recovery receipt and authority failures. These do not establish active-owner failover or successful live rollback.

Before production/mainnet, record outcomes for:

- Post-activation same-connection Claude and SDK/Lorebook result/ACK; cold/warm classification from observed sandbox/cache state, not elapsed time alone.
- MCP, SDK and prewarm co-placement; concurrent first calls creating one owner startup; added scopes/versions hydrated before execution.
- Active-owner drain/failover, stale execution/result denial, worker/controller restart and monotonic generations. Distinguish an actual partition from a stopped process.
- Protected reverse migration, transparent rollback for fleet-created/epoch-advanced owners, fresh legacy approval/revocation followed by restart, and forward recovery.
- Fresh owner onboarding, ordinary OAuth/PKCE, no-browser cold wake, ungranted/cross-owner/revocation denial, and retained full-PS/Lite/direct-read compatibility.
- Production capacity, boot/recovery time budgets, certificate renewal monitoring, signed-config renewal ownership, state backup/recovery and the controller availability dependency.

The current three-small-CVM pilot and its preview domains are not a production sizing, spend or retention policy. Record the approved rollout window and resource disposition separately; do not stop or destroy a state-bearing instance before service recovery is established.

## 12. Production readiness — net-new fleet

This is the current preparation gate, following the successful September9 pilot. It authorizes no production mutation or main/dev merge. Prepare production artifacts and evidence before executing the cross-surface Moksha release order in §1.

### 12.1 Durable trust and unattended certificate operation

The imminent pilot expiry is **signed boot configuration**, not an HTTPS certificate: `packages/enclave/src/fleet/security-config.ts` requires `issuedAt`/`expiresAt`, caps validity at24hours and checks it at every process startup. Merely leaving the current processes running does not make restart/recovery production-ready.

Replace this pilot authorization with a reviewed durable signed deployment-policy lifecycle. Normal restart must not require a person to mint another daily bundle. Preserve configuration authenticity, exact role/app/instance binding, fresh peer attestation, and an explicit policy for superseding or revoking old configuration and signing keys. Decide and test the replay/downgrade boundary before removing time-based rejection; the current implementation already disclaims hardware-backed disk-rollback protection. Keep the signing authority out of guest VMs and move operational credentials from a developer's Keychain to the deployment secret-management system. This is a required design/code change, not a completed capability or permission to disable verification.

Public HTTPS should retain normal automatically renewed certificates and persistent in-TEE ACME account/certificate storage. The current measured ingress mounts `/etc/letsencrypt`; [Phala's ingress documentation](https://docs.phala.com/phala-cloud/networking/setup-custom-domain) describes automatic renewal checks and persistent certificate volumes. Verify renewal and certificate reload with a safe test, restart persistence, expiry/failure alerting and the DNS/CAA procedure for a controller replacement. There must be no routine manual certificate-renewal step. External certificate lifetimes still apply.

### 12.2 Required work before launch

| Priority     | Work                                                                     | Completion evidence                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------ | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P0           | **Durable boot policy and managed key lifecycle** (§12.1)                | Restart after the pilot's former24hour boundary succeeds unattended; tampered, revoked and disallowed older policy is rejected under the documented threat model. Certificate renewal/reload and state persistence pass separately.                                                                                                                                                                                                                  |
| P0           | **Build release artifacts in CI, boot immutable images**                 | Controller and worker images contain the compiled enclave runtime and dependencies. No `git fetch`, `npm ci` or TypeScript build at guest startup. Pin every runtime/sandbox image by digest, record source provenance and approved measurements, and measure cold/restart times on the final artifacts. Both current fleet compose templates still build at boot.                                                                                   |
| P0           | **One reproducible production deployment path**                          | Version-controlled render, sign, deploy, attest, admit and health-check commands replace ad-hoc temporary helpers. Safe retries and partial-failure handling are demonstrated. A clean controller starts with signed `MCP_MIGRATION_REQUIRED=0` and needs no demo-state import. All roles run the reviewed release artifacts; the pilot currently mixes `79ca1fc` and `e33aca7`, with net-new bootstrap head `7b16465` not yet deployed.             |
| P0           | **Production trust anchors and release integration**                     | SDK production anchors are provisioned for the intended chain and worker identity; current `vana-sdk-combined` still has empty anchors for14800 and1480. Publish/re-pin the reviewed SDK, integrate fleet PRs into the feature parents, and validate Web/Account/Gateway/Storage/Lorebook against exact release refs. Production verification must succeed without preview anchor overrides or Vercel protection-bypass credentials.                 |
| P0           | **Controller recovery and fleet release rollback**                       | Recover encrypted OAuth/controller state onto an approved replacement, retain owner generations and single-writer fencing, and restore the stable MCP endpoint. Test release rollback within the fleet. Define backup custody, acceptable data loss and recovery time. Worker failover does not cover loss of the sole controller or its volume. Automatic multi-controller HA is not assumed or required unless the availability target demands it. |
| P0           | **Close the remaining user/security acceptance gaps on the final build** | Brand-new owner identity/setup, fresh OAuth, SDK delivery/ACK, concurrent MCP/SDK/prewarm causing one owner startup, scope/version refresh, revocation and cross-owner denial. Include worker loss during a running SDK job with bounded retry and stale-result rejection, and controller restart/replacement. Existing pilot MCP/SDK co-placement and MCP worker-stop recovery remain supporting evidence, not substitutes for these cases.         |
| Rollout gate | **Audit the actual production targets and migration ledgers**            | Verify each target database and all unapplied SQL, including0057/0058; resolve Account008/010 ledger ambiguity. Resolve Storage's `dp-rpc-dev.vana.org` versus canonical Moksha Gateway mapping. Verify contracts, public request audiences, OAuth/Privy settings, production flags, secret custody and stable domains. Historical environment inventory is not current target evidence.                                                             |
| Rollout gate | **Operational limits and visibility**                                    | Set measured capacity and cost limits, request/job concurrency limits, result retention/lifecycle, startup and recovery budgets. Alert on controller/worker loss, queue age, failed/retried jobs, attestation/config rejection, certificate expiry/renewal failure and storage errors. Exercise one unattended soak and verify alerts have a responsible recipient.                                                                                  |

The original SDK two-release sequence remains a provisional dependency order until production identities and anchor-publishing artifacts are fixed; reuse established identities only after verifying they are the intended production trust boundary. Mainnet additionally requires implementation and testing of chain1480 support: the current signed-config verifier explicitly accepts14800 only. Do not treat a successful production-hosted Moksha deployment as mainnet readiness.

### 12.3 Sequence and scope

1. Complete durable boot policy, immutable runtime packaging and a maintained deployment path together. Review the trust/revocation design before freezing production measurements.
2. Run one clean Moksha dress rehearsal on the final release artifacts: empty-controller activation, new-owner flows, security/failure cases, unattended restart/renewal and controller recovery. Preserve the existing pilot's useful evidence without using imported demo state as a fresh-start prerequisite.
3. Resolve production database/environment/anchor prerequisites and operational limits, then execute the dependency-ordered Moksha production plan. Mainnet follows its separate chain/contract/trust-anchor gates.

Autoscaling and automatic controller HA can follow once the initial capacity and availability targets are explicit. Full legacy single-worker restoration is not a launch prerequisite for this net-new fleet; supported production rollback must preserve the fleet's state and authority guarantees.
