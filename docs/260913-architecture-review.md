# TEE architecture review — 2026-09-13

**Superseded launch ordering — September 14:** architecture review (Task 1) is complete. Follow the [current production plan](260906-production-plan.md) and [Claude Fable launch handoff](260914-claude-fable-launch-handoff.md): deploy with one chain-aware rollout policy and retained mainnet Lite, prove rollout and selected mainnet smoke, expand, then retire Lite. The old Task 2 order and six-decision approval gate below are historical; do not repeat the review or require all six approvals. Larger findings remain follow-up work unless a concrete launch blocker emerges. Existing targeted operational hard stops remain.

Frozen review: PS `919932f`, Gateway `c7c73ba`, Unity `df5618f`; runtime unproven. `S/M/L` estimate: ≤1/2–4/≥5 days. Details: [receipt](../../e2e-proof-2026-09-09/fleet-overnight/reviews/architecture-review-detail.md). Six historical recommendations; current launch ordering is governed by the notice above.

| Rank | Severity / area  | Defect or decision                                                                                                                     | Frozen anchor                                                                                                        | Smallest fix                                                                    | Size |
| ---- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ---- |
| 1    | P1 availability  | A slow expired-owner cleanup can delay the next all-owner renew pass past another healthy owner's authority.                           | PS `packages/enclave/src/central/bootstrap.ts:445-460`; `packages/enclave/src/fleet/placement.ts:582-606`            | Isolate live renew cadence from cleanup/projection; prove stalled-peer slack.   | M    |
| 2    | P1 public work   | Anonymous peer challenge reservation races the 256 cap; unknown sessions buffer 12 MiB before lookup.                                  | PS `packages/enclave/src/fleet/peer.ts:267-298`                                                                      | Reserve permits pre-await; cap body/quote concurrency and deadlines.            | M    |
| 3    | P1 public work   | OAuth authorize creates unbounded durable pending state and holds a global mutation queue across remote readiness.                     | PS `packages/core/src/mcp/connection-api.ts:357-384`; `packages/server/src/mcp/durable-state.ts:189-204`             | Rate/count cap and purge pending state; bound queue.                            | M    |
| 4    | P1 traffic       | Owner slots do not limit concurrent MCP calls; unbounded placement queue; deadline starts after preparation.                           | PS `packages/enclave/src/fleet/placement.ts:86,437,462`; `packages/enclave/src/fleet/router.ts:69-87`                | Per-owner/global in-flight and end-to-end deadline.                             | M    |
| 5    | P1 data plane    | Hydration fully buffers ciphertext/plaintext/parse output without a supported working-set budget.                                      | PS `packages/core/src/sync/workers/download.ts:300-347`; `packages/enclave/src/sandbox/docker-runtime.ts:419-435`    | Stream/output/parser/scratch/concurrent-hydrate budgets; deterministic error.   | M    |
| 6    | P1 auth/MCP      | Fleet ingress has no owner-authenticated per-connection revoke.                                                                        | PS `packages/server/src/mcp/tee-ingress.ts:213-254`; `packages/core/src/mcp/connection-api.ts:235-249`               | Narrow signed list/revoke endpoint with replay fence and UI.                    | M    |
| 7    | P1 trust/ops     | Gateway code requires full controller-admin plus provider credential; actual provider IAM scope is unverified.                         | GW `lib/fleet/pool/loop.ts:48-54`; `lib/fleet/pool/phala.ts:11-12`                                                   | Restricted reconciler for listed-member lifecycle only.                         | M    |
| 8    | P2 data plane    | Valid approval of 33 distinct scopes later fails every data call at the 32-scope route cap.                                            | PS `packages/server/src/mcp/tee-ingress.ts:150-165`; `packages/server/src/routes/enclave-fleet.ts:30-44`             | One approval/controller/route scope ceiling and typed error.                    | S    |
| 9    | P2 availability  | Scaler repairs and new capacity share pending state; failed member can suppress spare start.                                           | GW `lib/fleet/pool/decision.ts:483,512,533,542`                                                                      | Measured warm reserve; separate repair from scale-up.                           | S/M  |
| 10   | P2 data plane    | Every data call prepares and executes all grant scopes; unchanged data normally dedups blob download but repeats readiness/queue work. | PS `packages/enclave/src/fleet/placement.ts:450-466`; `packages/enclave/src/fleet/worker-backend.ts:155-163,189-193` | Reuse fresh readiness; route only tool-required scopes.                         | M    |
| 11   | P2 observability | Pool reports planned admit/drain as 200 despite controller failure; recovery is safe/retried.                                          | GW `lib/fleet/pool/loop.ts:123-135,253-271`; GW `api/v1/cron/pool-loop.ts:57-63`                                     | Requested/acknowledged/observed action status and alert.                        | S    |
| 12   | P2 auth          | R0→R1→R2 makes replay of R0 miss family revocation while R2 remains usable.                                                            | PS `packages/core/src/mcp/connection-api.ts:725-744,805-811`; `packages/core/src/mcp/token-expiry.ts:59-77`          | Retain bounded family lineage; test R0→R1→R2→R0.                                | M    |
| 13   | Decision audit   | Access records are fail-open and process-memory buffered; three failures/overflow/restart can leave feed gaps.                         | PS `packages/server/src/jobs/access-reporter.ts:104-126,251-264`                                                     | Drop monitoring now; durable encrypted outbox only if completeness is promised. | M    |

1. **Bounded launch:** accept planned controller outage and defer ingress split/HA; require renewal, cold/warm, worker-loss and rollback proof on the chosen target.
2. **Trust:** explicitly accept Gateway revocation authority and operator app-KMS authority for this launch; redesign only if the product promises independence.
3. **Workload:** set a supported scope/owner profile only after 1/2/4 concurrent cold owners mixed with warm SDK/MCP, one worker loss, renewal slack and memory measurements; defer cap 6.
4. **Operations:** place lifecycle credentials in a narrow reconciler and require durable cron plus action/failure alerts before broad onboarding.
5. **MCP:** ship per-connection revoke and complete refresh-family reuse detection while preserving POST-only reconnect compatibility; streaming/resumability is later scope.
6. **Audit:** use best-effort access history with drop monitoring for the bounded launch; fund the durable outbox only if completeness is required.
